/**
 *  @author (Your name goes here!)
 *  This program is testing your ability to write methods to manipulate arrays.
 */
 
import java.util.*;

public class ArrayManipulations {

	public static void main(String[] args) {
		// You may change the code in main to more thoroughly test your methods

		// a few statements to test Part a

//		 String[] holidayWords = {"Santa", "sleigh", "Scrooge", "Merry", "Christmas"};
//		 System.out.println("Original words: " + Arrays.toString(holidayWords));
//		 startsWithS(holidayWords, "Ho");
//		 System.out.println("After method call: " + Arrays.toString(holidayWords));
//		 System.out.println("\n\n");

		
		// A few statements to test Part b
		
//		 int[][] table = { { 1, 2, 3, 4 }, { 0, 9, 5, 7 }, { 3, 3, 2, 1 }, {
//		 5, 6, 8, 7 }, { 4, 3, 7, 1 } };
//		 System.out.println("Original 2D array: " + Arrays.deepToString(table));
//		 reverseColumn(table, 2);
//		 System.out.println("AfterMethod call: " + Arrays.deepToString(table));
//		 System.out.println("\n\n");
		
		
		// a few statements to test Part c

//		 int[] list1 = {-9, 4, 12, 5, -1};
//		 int[] list2 = {5, 2,12, 7,-4, 10, 9, 5, 1};
//		 System.out.println("list1: " + Arrays.toString(list1));
//		 System.out.println("list2: " + Arrays.toString(list2));
//		 int[] result = greater(list1, list2);
//		 System.out.println("result: " + Arrays.toString(result));
	}


	/**
	 * Part a
	 * 
	 * Method startsWithS replaces all strings in an array that begin with
	 * "S" or "s" with another string
	 * 
	 * @param array-the array of strings to update each string that begins with "S" or "s"
	 * @param replacement- the string that will replace those that begin with "S" or "s"
	 */
	
	
	
	
	
	/**
	 * Part b
	 * 
	 * Method reverseColumn reverses the elements of one column of a 2D array
	 * 
	 * @param matrix- the 2D array with will have one column reversed
	 * @param index- the index of the column to be reversed
	 * @throws IllegalArgumentException if the column index is invalid
	 */

	
	
	
	
	

	/**
	 * Part c
	 * 
	 * Method greater creates a new array whose elements are the greater integer
	 * at between the elements of two other arrays at each index. If one array is
	 * longer, then the remaining elements of that array are copied to the new array at
	 * the corresponding indexes
	 * 
	 * @param list1 - one of the 1D arrays to be compared
	 * @param list2 - the second of the 1D arrays to be compared
	 * @return an array with the larger element from list1 and list2 at each index
	 * 
	 */
	
	
}
